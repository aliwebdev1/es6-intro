const add = (numbur, number1) => numbur + number1;

const result = add(22, 88);

const multiply = (num1, num2, num3) => num1 * num2 * num3;
const rsult = multiply(3, 3, 3);
console.log(rsult);

const tenTimes = (num) => num * 10;
const output = tenTimes(15);

const fiveTimes = num => num * 5;
const value = fiveTimes(10);

const doMath = (x, y) => {
    const add = x + y;
    const minus = x - y;
    const multi = add * minus;
    return multi
}

const folafol = doMath(5, 3);
console.log(folafol);